abstracts/

Purpose: Store design tokens and helpers used across the whole project.

What to put here:
- _variables.scss: color, spacing, font variables
- _mixins.scss: Sass mixins for reuse
- _functions.scss: Sass functions

Notes:
- Do not output any CSS from this folder (no selectors). Only variables, mixins, and functions.
